name: api_sdk
description: "Shared models, providers, theme, and widgets for health platform."
version: 0.0.1
homepage:

resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  intl: ^0.20.2
  flutter_riverpod: ^3.2.1
  go_router: ^14.6.0
  supabase_flutter: ^2.8.0
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand/
